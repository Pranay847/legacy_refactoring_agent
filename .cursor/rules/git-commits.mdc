---
description: Git commit rules — no Cursor attribution
alwaysApply: true
---

Never add "Co-authored-by: Cursor", "Made-with: Cursor", or any Cursor attribution trailer to git commits or pull requests.

When committing, use plain `git commit -m "message"` only. Do not use `--trailer` flags.
